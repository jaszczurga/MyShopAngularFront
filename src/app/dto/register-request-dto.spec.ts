import { RegisterRequestDto } from './register-request-dto';

describe('RegisterRequestDto', () => {
  it('should create an instance', () => {
    expect(new RegisterRequestDto()).toBeTruthy();
  });
});
